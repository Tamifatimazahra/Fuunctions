const prompt = require("prompt-sync")();
function filterPairs (tab){
    tab =[1,2,3,4,5,6 ]; 
    for (i=0;i<tab.length ;i++){
        if(tab[i]%2===0)
         
    console.log(tab[i]);

}
// return tab[i]}
}
filterPairs();
